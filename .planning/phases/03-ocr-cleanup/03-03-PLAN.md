---
phase: 03-ocr-cleanup
plan: 03
type: execute
wave: 6
depends_on: ["03-02"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [FR-06]

must_haves:
  truths:
    - "CLAUDE.md accurately documents Florence-2 + CaptionModel + TranslationModel architecture"
    - "File Map reflects new model files and deleted old ones"
    - "Request Flow shows Florence/Caption/OCR pipeline"
    - "Configuration section includes ENABLE_OCR"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project documentation reflecting new architecture"
      contains: "florence_model.py"
  key_links: []
---

<objective>
Update CLAUDE.md to reflect the completed Vision Intelligence Upgrade — new model architecture, updated file map, request flow, and configuration.

Purpose: Keep project documentation accurate so future development is guided by correct architecture descriptions.
Output: Updated CLAUDE.md
</objective>

<execution_context>
@/home/debian/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/debian/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md architecture documentation</name>
  <files>CLAUDE.md</files>
  <action>
Update the following sections in CLAUDE.md:

1. **File Map table** — replace old model entries:
   - Remove `models/openclip_model.py` and `models/blip_model.py` rows
   - Add:
     - `models/florence_model.py` → "Open-vocabulary tagging via Florence-2 `<OD>` + `<DENSE_REGION_CAPTION>`, object detection, OCR"
     - `models/caption_model.py` → "Image captioning via Florence-2 `<MORE_DETAILED_CAPTION>` + opus-mt translation, name injection, school age detection"
     - `models/translation_model.py` → "English → Indonesian translation wrapper (Helsinki-NLP/opus-mt-en-id)"

2. **Architecture > Request Flow** — update to:
   ```
   Image URL → ImageDownloader → InsightFace (detect faces + 512-dim embeddings)
                               → ClusteringService → ChromaDB (find/create cluster)
                               → ThumbnailService (save face crop if new cluster)
                               → FlorenceModel (open-vocabulary tags + English objects)
                               → CaptionModel (English caption → Indonesian description, name injection)
                               → FlorenceModel OCR (optional, when ENABLE_OCR=true)
                               → Merge context elements into tags
   ```

3. **Configuration section** — add `ENABLE_OCR=false` line with comment, remove `CONTEXT_MODE` if present

4. Do NOT change Commands, Testing, or other unaffected sections
  </action>
  <verify>
grep "florence_model.py" CLAUDE.md && echo "Florence documented" || echo "FAIL: Florence missing"
grep "caption_model.py" CLAUDE.md && echo "Caption documented" || echo "FAIL: Caption missing"
grep "translation_model.py" CLAUDE.md && echo "Translation documented" || echo "FAIL: Translation missing"
grep "openclip_model.py" CLAUDE.md && echo "FAIL: Old openclip still in docs" || echo "Old openclip removed"
grep "blip_model.py" CLAUDE.md && echo "FAIL: Old blip still in docs" || echo "Old blip removed"
grep "ENABLE_OCR" CLAUDE.md && echo "OCR config documented" || echo "FAIL: OCR config missing"
  </verify>
  <done>
- CLAUDE.md File Map shows florence_model.py, caption_model.py, translation_model.py
- No references to openclip_model.py or blip_model.py
- Request Flow reflects new pipeline
- ENABLE_OCR documented in Configuration
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CLAUDE.md File Map has 3 new model entries, 0 old model entries
- [ ] Request Flow mentions FlorenceModel, CaptionModel, OCR
- [ ] Configuration has ENABLE_OCR
- [ ] No mention of OpenCLIP or BLIP as active models
</verification>

<success_criteria>
- CLAUDE.md accurately reflects post-migration architecture
- New developers reading CLAUDE.md get correct understanding of model stack
</success_criteria>

<output>
After completion, create `.planning/phases/03-ocr-cleanup/03-03-SUMMARY.md`
</output>
