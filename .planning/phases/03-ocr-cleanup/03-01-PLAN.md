---
phase: 03-ocr-cleanup
plan: 01
type: execute
wave: 5
depends_on: ["02-01", "02-02"]
files_modified:
  - python-worker/models/florence_model.py
  - python-worker/config.py
  - python-worker/api/schemas.py
  - python-worker/api/routes.py
  - python-worker/.env.example
  - python-worker/tests/test_models.py
autonomous: true
requirements: [FR-05, FR-06, NFR-03]

must_haves:
  truths:
    - "ENABLE_OCR=true populates ProcessResponse.ocr.text for images with text"
    - "ENABLE_OCR=false (default) returns ProcessResponse.ocr as null"
    - "OCR errors never crash the processing pipeline"
    - "All existing ProcessResponse fields unchanged (non-breaking)"
  artifacts:
    - path: "python-worker/api/schemas.py"
      provides: "OcrRegion + OcrResult schemas, ProcessResponse.ocr field"
      contains: "OcrResult"
    - path: "python-worker/config.py"
      provides: "ENABLE_OCR config setting"
      contains: "ENABLE_OCR"
  key_links:
    - from: "python-worker/api/routes.py"
      to: "python-worker/models/florence_model.py"
      via: "calls florence_model.get_ocr(image) when ENABLE_OCR=true"
      pattern: "florence_model\\.get_ocr"
    - from: "python-worker/api/routes.py"
      to: "python-worker/api/schemas.py"
      via: "builds OcrResult from get_ocr() output"
      pattern: "ocr_result"
---

<objective>
Add `get_ocr()` method to FlorenceModel, create OcrResult schema, add ENABLE_OCR config, and wire OCR into the processing pipeline.

Purpose: Expose Florence-2's native OCR as an optional field in ProcessResponse. Zero-cost when disabled (default), non-breaking addition for existing API consumers.
Output: OCR method + schema + config + pipeline wiring
</objective>

<execution_context>
@/home/debian/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/debian/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@python-worker/models/florence_model.py
@python-worker/config.py
@python-worker/api/schemas.py
@python-worker/api/routes.py
@python-worker/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_ocr() to FlorenceModel and ENABLE_OCR config</name>
  <files>python-worker/models/florence_model.py, python-worker/config.py, python-worker/.env.example</files>
  <action>
1. Add `ENABLE_OCR` to `config.py` after `ENABLE_AGE_DETECTION`:
   ```python
   ENABLE_OCR: bool = os.getenv("ENABLE_OCR", "false").lower() == "true"
   ```

2. Add `ENABLE_OCR=false` line to `.env.example` with comment.

3. Add `get_ocr(image, with_regions=False)` method to FlorenceModel:
   - `with_regions=False` → runs `<OCR>` task, returns `{"text": str}`
   - `with_regions=True` → runs `<OCR_WITH_REGION>` task, returns `{"text": str, "regions": [{"text": str, "bbox": [8 coords]}]}`
   - Florence-2 `<OCR>` returns `{"<OCR>": "text"}` — extract string
   - Florence-2 `<OCR_WITH_REGION>` returns `{"<OCR_WITH_REGION>": {"quad_boxes": [...], "labels": [...]}}` — zip into regions list, join labels for full text
   - On error: log and return `{"text": ""}` — never raise, never return None
  </action>
  <verify>
cd python-worker && python -c "
from config import Settings
s = Settings()
print(f'ENABLE_OCR default: {s.ENABLE_OCR}')

from models.florence_model import FlorenceModel
from models.translation_model import TranslationModel
from PIL import Image
import numpy as np

trans = TranslationModel()
fm = FlorenceModel(translation_model=trans, device_type='cpu')
img = Image.fromarray(np.random.randint(0, 255, (480, 640, 3), dtype=np.uint8))
result = fm.get_ocr(img)
print(f'OCR result keys: {list(result.keys())}')
print(f'OCR text type: {type(result[\"text\"])}')
"
  </verify>
  <done>
- ENABLE_OCR defaults to false in config
- get_ocr() returns dict with "text" key
- get_ocr(with_regions=True) returns dict with "text" + "regions" keys
- Errors return {"text": ""} without crashing
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OcrResult schema, wire into routes.py pipeline, add tests</name>
  <files>python-worker/api/schemas.py, python-worker/api/routes.py, python-worker/tests/test_models.py</files>
  <action>
1. In `api/schemas.py`:
   - Add `OcrRegion(BaseModel)` with `text: str` and `bbox: List[float]`
   - Add `OcrResult(BaseModel)` with `text: str` and `regions: Optional[List[OcrRegion]]`
   - Add `ocr: Optional[OcrResult] = Field(None, ...)` as LAST field in `ProcessResponse` (non-breaking additive change)

2. In `api/routes.py` `process_image_handler()`:
   - After context generation (step 7), add OCR step:
     ```python
     ocr_result = None
     if settings.ENABLE_OCR:
         try:
             ocr_data = florence_model.get_ocr(image, with_regions=False)
             if ocr_data.get("text"):
                 ocr_result = ocr_data
         except Exception as e:
             logger.error(f"OCR failed (non-fatal): {e}")
             ocr_result = None
     ```
   - Add `"ocr": ocr_result` to the result dict

3. In `tests/test_models.py`:
   - Add `TestFlorenceModelOCR` class:
     - `test_get_ocr_returns_dict` — asserts dict with "text" key
     - `test_get_ocr_with_regions` — asserts "regions" key present
     - `test_get_ocr_graceful_failure` — tiny 1x1 image doesn't crash
   - Add schema test: `OcrResult` is importable, `ProcessResponse` has `ocr` field
  </action>
  <verify>
cd python-worker && pytest tests/test_models.py -v 2>&1 | tail -20
  </verify>
  <done>
- OcrResult and OcrRegion schemas importable from api.schemas
- ProcessResponse has ocr: Optional[OcrResult] field
- OCR step in routes.py runs only when ENABLE_OCR=true
- OCR errors don't crash pipeline
- All tests pass
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from api.schemas import OcrResult, OcrRegion, ProcessResponse"` succeeds
- [ ] `ProcessResponse.model_fields['ocr']` exists and is Optional
- [ ] `grep "ENABLE_OCR" python-worker/config.py` returns the config line
- [ ] `pytest tests/test_models.py -v` passes
- [ ] `grep "ocr_result" python-worker/api/routes.py` shows pipeline wiring
</verification>

<success_criteria>
- OCR works end-to-end when ENABLE_OCR=true
- OCR is null when ENABLE_OCR=false (default)
- No breaking changes to existing ProcessResponse fields
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ocr-cleanup/03-01-SUMMARY.md`
</output>
