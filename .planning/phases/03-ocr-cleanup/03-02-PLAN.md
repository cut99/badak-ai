---
phase: 03-ocr-cleanup
plan: 02
type: execute
wave: 6
depends_on: ["03-01"]
files_modified:
  - python-worker/models/openclip_model.py
  - python-worker/models/blip_model.py
  - python-worker/requirements.txt
  - python-worker/config.py
  - python-worker/.env.example
  - python-worker/api/schemas.py
  - python-worker/tests/test_models.py
  - python-worker/tests/test_api.py
autonomous: true
requirements: [FR-06, NFR-02, NFR-03]

must_haves:
  truths:
    - "openclip_model.py and blip_model.py are deleted"
    - "open-clip-torch removed from requirements.txt"
    - "No stale imports of OpenCLIPModel or BLIPModel anywhere in codebase"
    - "pytest tests/ passes (full suite)"
    - "Worker starts successfully without old model files"
  artifacts: []
  key_links: []
---

<objective>
Delete old model files (openclip_model.py, blip_model.py), remove open-clip-torch dependency, clean up dead config, update health check schema, verify no stale references.

Purpose: Complete the migration by removing all dead code from the old OpenCLIP + BLIP architecture.
Output: Clean codebase with only Florence-2 + CaptionModel + TranslationModel
</objective>

<execution_context>
@/home/debian/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/debian/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@python-worker/models/openclip_model.py
@python-worker/models/blip_model.py
@python-worker/requirements.txt
@python-worker/config.py
@python-worker/.env.example
@python-worker/api/schemas.py
@python-worker/tests/test_models.py
@python-worker/tests/test_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no stale imports, delete old files, clean dependencies</name>
  <files>python-worker/models/openclip_model.py, python-worker/models/blip_model.py, python-worker/requirements.txt, python-worker/config.py, python-worker/.env.example</files>
  <action>
1. FIRST verify no remaining imports:
   ```bash
   grep -rn "openclip_model\|OpenCLIPModel\|blip_model\|BLIPModel" --include="*.py" python-worker/
   ```
   If any remain, fix them before proceeding.

2. Delete old model files:
   - `rm python-worker/models/openclip_model.py`
   - `rm python-worker/models/blip_model.py`

3. Remove `open-clip-torch==2.24.0` from `requirements.txt`

4. Remove dead `CONTEXT_MODE` config from `config.py`:
   - Delete the line `CONTEXT_MODE: str = os.getenv("CONTEXT_MODE", "comprehensive")`

5. Remove `CONTEXT_MODE` from `.env.example` if present
  </action>
  <verify>
ls python-worker/models/openclip_model.py 2>&1 | grep -q "No such file" && echo "openclip deleted" || echo "FAIL: openclip still exists"
ls python-worker/models/blip_model.py 2>&1 | grep -q "No such file" && echo "blip deleted" || echo "FAIL: blip still exists"
grep "open-clip-torch" python-worker/requirements.txt && echo "FAIL: open-clip-torch still in requirements" || echo "open-clip-torch removed"
grep "CONTEXT_MODE" python-worker/config.py && echo "FAIL: CONTEXT_MODE still in config" || echo "CONTEXT_MODE removed"
grep -rn "openclip_model\|OpenCLIPModel\|blip_model\|BLIPModel" --include="*.py" python-worker/ && echo "FAIL: stale imports found" || echo "No stale imports"
  </verify>
  <done>
- openclip_model.py deleted
- blip_model.py deleted
- open-clip-torch removed from requirements.txt
- CONTEXT_MODE dead config removed
- Zero stale imports of old models in any .py file
  </done>
</task>

<task type="auto">
  <name>Task 2: Update schemas, fix test references, run full test suite</name>
  <files>python-worker/api/schemas.py, python-worker/tests/test_models.py, python-worker/tests/test_api.py</files>
  <action>
1. In `api/schemas.py`:
   - Update `HealthResponse` example to show `florence`, `caption`, `translation` (not `openclip`, `blip`)

2. In `tests/test_models.py`:
   - Verify no test imports `OpenCLIPModel` or `BLIPModel` — remove any remaining
   - Verify `TestModelIntegration` uses `FlorenceModel` + `CaptionModel`

3. In `tests/test_api.py`:
   - Verify health check test doesn't assert `"openclip"` or `"blip"` in models_loaded
   - Update to assert `"florence"` and `"caption"` if needed
   - Check mock targets — if any test mocks `routes.openclip_model` or `routes.blip_model`, update to `routes.florence_model` or `routes.caption_model`

4. Run full test suite: `pytest tests/ -v`
  </action>
  <verify>
cd python-worker && pytest tests/ -v 2>&1 | tail -30
  </verify>
  <done>
- HealthResponse example reflects new model names
- No test references old model classes
- Mock targets updated for new model names
- pytest tests/ passes (all suites — test_api, test_models, test_clustering)
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ls python-worker/models/openclip_model.py` fails (file not found)
- [ ] `ls python-worker/models/blip_model.py` fails (file not found)
- [ ] `grep -rn "openclip\|OpenCLIP\|blip_model\|BLIPModel\|open-clip" --include="*.py" python-worker/` returns zero
- [ ] `grep "open-clip-torch" python-worker/requirements.txt` returns zero
- [ ] `pytest tests/ -v` all pass
</verification>

<success_criteria>
- Old model files completely removed
- No dead dependencies in requirements.txt
- No stale imports or references anywhere
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ocr-cleanup/03-02-SUMMARY.md`
</output>
